<h4>Catégories de produits</h4>
<% shop.product_categories.distinct.each do |product_category| %>
  <%= link_to anchor: "cat-#{product_category.id}" do %>
    <p><%= product_category.name %></p>
  <% end %>
<% end %>
